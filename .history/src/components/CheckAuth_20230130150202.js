import { useEffect } from 'react'
import { useSession } from 'next-auth/client'

const CheckAuth = () => {
    const [ session ] = useSession()

    useEffect(() => {
        if()
    }[session])

    return (
    
    )
}